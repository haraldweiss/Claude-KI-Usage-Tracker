# Production build target: VPS subpath under wolfinisoftware.de.
# Apache reverse-proxies /claudetracker/api/ → http://127.0.0.1:3001/api/,
# so the frontend uses a same-origin relative URL.
VITE_API_URL=/claudetracker
